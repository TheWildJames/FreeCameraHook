pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        // Add JitPack repository for Xposed API
        maven { url 'https://jitpack.io' }
        // Add alternative repository for Xposed
        maven { url 'https://api.xposed.info/' }
    }
}

rootProject.name = "CameraInterceptor"
include ':app'